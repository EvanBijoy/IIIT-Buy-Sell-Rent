import { Center } from '@mantine/core';
import logo from './image.png';

const IIITLogo = () => {
  return (
    <Center>
      <img src={ logo } alt="IIIT Logo" width={50} height={50} />
    </Center>
  );
};

export default IIITLogo;
